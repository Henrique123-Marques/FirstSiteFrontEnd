$('.slider-principal').slick({
    dots: true,
    infinite: true,
    speed: 300,
    slidesToShow: 1, //quantos slides iram passar em uso
    adaptiveHeight: true,
    autoplay: true,
    autoplaySpeed: 3000
  }); 